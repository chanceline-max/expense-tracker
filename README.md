# 个人记账 Web 应用

一个使用 Flask 构建的个人记账应用，正在逐步开发中。

## 当前状态

当前版本已经搭建基础页面，下一步将加入 SQLite 数据库和记账表单。

## 本地运行

```bash
python -m venv .venv
.venv\\Scripts\\activate
pip install -r requirements.txt
python app.py
```

然后打开 <http://127.0.0.1:5000>。
